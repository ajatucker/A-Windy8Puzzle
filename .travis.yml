language: python

matrix:
  include:
    - name: "Python 3.8 dev"
      python: 3.8-dev
      dist: xenial
      before_install:
        - python -m pip install --upgrade pip
      install:
        pip install --user -r requirements.txt --use-feature=2020-resolver
